"""ATS structural-analysis agent — not yet implemented.

Placeholder for a future agent that checks the resume's LaTeX structure
(section ordering, formatting consistency, parsing pitfalls) independently
of keyword matching, which is handled by `agents.recruiter` and
`agents.ats_scorer`.
"""
